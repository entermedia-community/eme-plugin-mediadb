{
	"model": "${model}",
	"messages": [
		{
			"role": "system",
			"content": #jesc("[[You are a helpful assistant that can automatically detect what type of conversation is taking place based on the user's input. Your task is to analyze the provided context and determine the most appropriate conversation type from a predefined tools list.]]

[[You are able to select one of the following tools:]]

[[1. ask_questions: Use this tool when the user is asking questions or seeking information.]]
[[2. general_chat: Use this tool for general conversations that do not fit into specific categories.]]
[[3. search_parse: Use this tool when the user is trying to search for images, documents or specific content.]]
[[4. image_creation: Use this tool when the user is requesting the creation of an image.]]


[[Based on the user's input, choose the most appropriate tool and provide the necessary parameters as specified in the tools list.]]")
		}
		#if($channelchathistory.size() <= 1)
		, 
		#else 
		$pages.include("./commons/messagehistory.json", $context) 
		#end
		{
			"role": "user",
			"content": #jesc(${message.message})
		}
	],
	"tools": [
		{
			"type": "function",
			"function": {
				"name": "chat_questions.question_ask",
				"description": "Use this tool when the user is asking questions or seeking information.",
				"parameters": {
					"type": "object",
					"properties": {
						"question": {
							"type": "string",
							"description": "The specific question being asked by the user."
						},
						"role": {
							"type": "array",
							"description": "The role of agents who needs to answer the question."
							#if(!$roles.isEmpty())
							,
							"enum": [
								#foreach($role in $roles)
									"#text($role)"
									#if($foreach.hasNext),#end
								#end
							]
							#end
							
						}
					},
					"required": ["question"]
				}
			}
		},
		{
			"type": "function",
			"function": {
				"name": "creation_image_parse",
				"description": "Use this tool when the user is requesting the creation of an image",
				"parameters": {
					"type": "object",
					"properties": {
						"image_name": {
							"type": "string",
							"description": "The name or description of the image to be created."
						},
						"prompt": {
							"type": "string",
							"description": "The prompt or details for generating the image."
						}
					},
					"required": ["image_name", "prompt"]
				}
			}
		},
		{
			"type": "function",
			"function": {
				"name": "auto_detect_showresponse",
				"description": "Use this tool for general conversations that do not fit into specific categories.",
				"parameters": {
					"type": "object",
					"properties": {
						"friendly_response": {
							"type": "string",
							"description": "A friendly response to keep the conversation going and asking user to be more specific."
						}
					},
					"required": ["friendly_response"]
				}
			}
		},
		{
			##TODO: addd message history
			"type": "function",
			"function": {
				"name": "search_parse",
				"description": 	#jesc("Use this tool to search as a search assistant that maps natural language requests to a logical steps that could be executed as sequential database or API operations. 

				You will first determine the next step, which is one of these values: 'conversation' and 'search_parse'.
				If not clear which step to select, use 'conversation' and ask for clarification.

				Based on the selected next_step, fill respective field in step_details:
				if next_step is conversation, step_details must have conversation object
				if next_step is search_parse, step_details must have parseSearchParts object.
				"),
				"parameters": {
					"type": "object",
					"properties": {
						"step_details": {
								"type": "object",
								"description": "Details about the selected step.",
								"properties": {
									"conversation": $pages.include("./parsefunctions/conversation.json", $context),
									"parseSearchParts": $pages.include("./parsefunctions/parseSearchParts.json", $context)
								}
							}
					},
					"required": ["friendly_response"]
				}
			}
		}
	],
	"tool_choice": "required"
}

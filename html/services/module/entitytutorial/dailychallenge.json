{
  "response": {
    "status": "ok",
    "userid": "$user.id"
  },
  "dailychallenge": {
    "id": #jesc($dailychallenge.id), 
    "challengedate": #jesc($dailychallenge.get("challengedate")),
    "sectionid": #jesc($dailychallenge.get("sectionid"))
  },
  "channelhistory": [
    #foreach($channel in $channelhistory)
      #if($foreach.count > 1),#end
      {
        "id": #jesc($channel.id),
        "channeltype": #jesc($channel.get("channeltype")),
        "name": #jesc($channel.get("name")),
        "dataid": #jesc($channel.get("dataid")),
        "searchtype": #jesc($channel.get("searchtype")),
        "user": #jesc($channel.get("user")),
        "refreshdate": #jesc($channel.get("refreshdate")),
        "channelstatus": #jesc($channel.get("channelstatus")),
        "date": #jesc($channel.get("date"))
      }
    #end
  ],
  #ifnotnull($activechannel)
  "activechannel": {
    "id": #jesc($activechannel.id),
    "channeltype": #jesc($activechannel.get("channeltype")),
    "name": #jesc($activechannel.get("name")),
    "dataid": #jesc($activechannel.get("dataid")),
    "searchtype": #jesc($activechannel.get("searchtype")),
    "user": #jesc($activechannel.get("user")),
    "refreshdate": #jesc($activechannel.get("refreshdate")),
    "channelstatus": #jesc($activechannel.get("channelstatus")),
    "date": #jesc($activechannel.get("date"))
  },
  #end
  #ifnotnull($currentchannel)
  "currentchannel": {
    "id": #jesc($currentchannel.id),
    "channeltype": #jesc($currentchannel.get("channeltype")),
    "name": #jesc($currentchannel.get("name")),
    "dataid": #jesc($currentchannel.get("dataid")),
    "searchtype": #jesc($currentchannel.get("searchtype")),
    "user": #jesc($currentchannel.get("user")),
    "refreshdate": #jesc($currentchannel.get("refreshdate")),
    "channelstatus": #jesc($currentchannel.get("channelstatus")),
    "date": #jesc($currentchannel.get("date"))
  },
  #end
  "messages": [
#set($count = 0)
#foreach($message in $messages)
  #if($message.messagetype != "system")
    #set($count = $count + 1)
    #if($count > 1),#end
    {
      "channel": #jesc($message.get("channel")),
      "date": #jesc($message.get("date")),
      "messageid": #jesc($message.get("id")),
      "message": #jesc($message.get("message")),
      "messagetype": #jesc($message.get("messagetype")),
      "replytoid": #jesc($message.get("replytoid")),
      "user": #jesc($message.get("user")),
      "agentcontextvalues": #jesc($message.get("agentcontextvalues"))
    }
  #end
#end
  ],
  "answers": [
  #foreach($answer in $answers)
    #if($foreach.count > 1),#end
    {
      "answerid": #jesc($answer.get("id")),
      "channel": #jesc($answer.get("channel")),
      "questionid": #jesc($answer.get("entityquestion")),
      "confidence": #jesc($answer.get("answerconfidence")),
      "selectedoption": #jesc($answer.get("selectedoption")),
      "iscorrect": #jesc($answer.get("iscorrect"))
    }
  #end
  ]
}
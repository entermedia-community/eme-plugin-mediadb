{
	#ifnotnull($user)
	"response": {
		"status": "ok",
		"userid": "$user.id"
	},
	"user": { 
		"id" : "$user.id",
		"firstname": "$!user.firstName",
		"lastname" : "$!user.lastName",
		"email" : "$!user.email",
		"screenname" : "$!user.getScreenName()"
	}
	#end

	#ifnull($user)
	"response": {
		"status": "error",
		"userid": null
	}
	#end
}

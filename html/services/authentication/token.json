{
	#if($commandSucceeded == "ok")
		"access_token": "$!{access_token}",
		"token_type": "$!{token_type}",
		"expires_in": $!{expires_in}
		#if($refresh_token),
		"refresh_token": "$!{refresh_token}"
		#end
		#if($user),
		"user": { 
			"id" : "$user.id",
			"firstname": "$!user.firstName",
			"lastname" : "$!user.lastName",
			"email" : "$!user.email",
			"screenname" : "$!user.getScreenName()" 
			#set($assetportrait = $mediaarchive.asLinkToPreview($userprofile.get("assetportrait"), "image200x200"))
			#ifnotnull($assetportrait),
			"assetportrait" : "$!siteroot$assetportrait"
			#end
		}
		#end
	#end

	#ifnotnull(oauth_error),
		"error": "$!{oauth_error}"
	#end
	#ifnotnull(oauth_error_description),
		"error_description": "$!{oauth_error_description}"
	#end
}
